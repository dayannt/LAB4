// Punto de entrada principal
import './components/AppContainer';

// Esto asegura que todos los componentes se inicialicen correctamente
document.addEventListener('DOMContentLoaded', () => {
  console.log('Aplicación de combates de Hora de Aventura iniciada');
});